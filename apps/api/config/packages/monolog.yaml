monolog:
  handlers:
    main:
      type: stream
      path: 'php://stderr'
      level: info
      formatter: monolog.formatter.json
      channels: ['!event']
    console:
      type: console
      process_psr_3_messages: false
      channels: ['!event', '!doctrine']
