nelmio_cors:
  defaults:
    origin_regex: false
    allow_origin: ['https://app.bulletin.local']
    allow_methods: ['GET', 'POST', 'OPTIONS']
    allow_headers: ['Content-Type', 'Accept-Language']
    expose_headers: []
    max_age: 3600
    allow_credentials: true
  paths:
    '^/': ~
