monolog:
  handlers:
    main:
      type: fingers_crossed
      action_level: error
      handler: nested
      channels: ['!event', '!doctrine']
    nested:
      type: stream
      path: php://stderr
      formatter: monolog.formatter.json
